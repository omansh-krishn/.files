#!/usr/bin/env /lib/runit/invoke-run
set -e

[ ! -d /run/proftpd ] && mkdir /run/proftpd
exec 2>&1
proftpd --configtest || exit 162

if [ -e /etc/runit/verbose ]; then
	echo "invoke-run: starting ${PWD##*/}"
fi
exec /usr/sbin/proftpd -n 
