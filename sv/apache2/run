#!/usr/bin/env /lib/runit/invoke-run

. /etc/apache2/envvars

exec 1>&2

apachectl configtest  || exit 162

if [ -e /etc/runit/verbose ]; then
	echo "invoke-run: starting ${PWD##*/}"
fi

exec  /usr/sbin/apache2 -DNO_DETACH
